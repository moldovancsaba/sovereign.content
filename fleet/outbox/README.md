# Outbox — per-agent recommendation packets

Daily SWOT writes `\<clientId\>-YYYY-MM-DD.md` here for handoff into that client's chat.

Packets must include problem → why → how → evidence → delivery class.
