# Per-country padel research seed notes

Agent research notes copied from `moldovancsaba/management` `docs/*-padel-research-seed.md`
on `release/padel-africa` during the core reconcile triage (2026-09-25).

These are **not** engine docs. Live fixtures live under `scripts/data/*-verified.json`.
Writes to the product go only via `ingest/client.ts` → `POST /api/ingest`.
