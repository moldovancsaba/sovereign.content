@sovereignsquad:registry=https://npm.pkg.github.com
# Optional for live registry upgrades. Production installs use file: vendor/*.tgz
# so Vercel does not need GitHub Packages auth.
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
